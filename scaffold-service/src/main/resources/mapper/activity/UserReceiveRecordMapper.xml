<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lantin.web.mapper.db1.activity.UserReceiveRecordMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.lantin.web.domain.activity.UserReceiveRecord">
        <id column="id" property="id" />
        <result column="record_id" property="recordId" />
        <result column="prize_id" property="prizeId" />
        <result column="receive_time" property="receiveTime" />
        <result column="uid" property="uid" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, record_id, prize_id, receive_time, uid
    </sql>

</mapper>
